# 입력 경계

이 실행은 작업공간 루트의 `input/`만 읽었다. 이 디렉터리는 원본을 복사하거나 수정하지 않고, 사용한 입력 경계와 해시를 `../frozen/manifest.json`에 기록하기 위한 안내 디렉터리다.

- 허용 입력: `input/career_run/**`, `input/경험정리/**`, `input/직무기술서/**`
- 제외 범위: 상위 OneDrive 자료, Downloads, 인터넷, `career_pipeline_max_quality/`
- 원본 변경: 없음

